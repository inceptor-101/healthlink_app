<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Online Consultation Form</title>
<style>
  body { font-family: Arial, sans-serif; }
  .container { width: 50%; margin: auto; padding: 20px; }
  .form-group { margin-bottom: 10px; }
  label { display: block; margin-bottom: 5px; }
  input[type="text"], input[type="tel"] { width: 100%; padding: 10px; margin-bottom: 10px; }
  .btn { background-color: purple; color: white; padding: 10px 20px; border: none; cursor: pointer; }
  .secure { display: flex; align-items: center; }
  .secure img { margin-right: 5px; }
</style>
</head>
<body>
<div class="container">
  <h2>Consult with a Doctor</h2>
  <form action="/consultWithDoctor" method="POST">
    <div class="form-group">
        <label for="symptom">Tell us your symptom or health problem</label>
        <input type="text" name="symptom" placeholder="Eg: fever, headache" required>
      </div>
      <div class="form-group">
        <label for="mobile">Mobile number</label>
        <input type="tel" name="mobile" placeholder="Enter mobile number" required>
        <small>A verification code will be sent to this number.</small>
      </div>
      <button class="btn" type="submit">Submit</button>
  </form>
</body>
</html>